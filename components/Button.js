import React from 'react';

import {Button as GravityButton} from '@gravity-ui/uikit/build/cjs';

import '@gravity-ui/uikit/build/cjs/components/Button/Button.css';

const Button = (props) => {
    return <GravityButton {...props} />;
};

export default Button;
