import React from 'react';

import {Card as GravityCard} from '@gravity-ui/uikit/build/cjs';

import '@gravity-ui/uikit/build/cjs/components/Card/Card.css';

const Card = (props) => {
    return <GravityCard {...props} />;
};

export default Card;
